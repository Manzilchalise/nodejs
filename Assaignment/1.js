let x=1 //This is variable declaration
for(i=0; i<50;i++) { //This is loop which runs for 50 iterations
    if(x%2==0) { //it checks whether the number is even or not
        console.log(x+"\n") //this blcok si executed for even no
    }x++ //this increments the value of x
}
